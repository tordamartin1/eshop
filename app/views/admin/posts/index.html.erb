<h1>Admin článkov</h1>

<p>
  <%= link_to "Pridať článok", new_admin_post_path, class: "btn btn-primary" %>
</p>

<table class="table">

  <thead>
    <tr>
      <th>Nazov</th>
      <th>Publikovane</th>
      <th>Akcie</th>
    </tr>
  </thead>

  <tbody>

    <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.published %></td>
      <td><%= link_to "Upravit", edit_admin_post_path(post) %></td>
    </tr>
    <% end %>

  </tbody>
</table>
